// given two arrays containing groups of characters
// return whether the arrays make equal strings when their respective groups are joined

const arrayStringsAreEqual = (word1, word2) => word1.join('') === word2.join('')